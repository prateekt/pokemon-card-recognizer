name: card_rec_env
channels:
  - defaults
dependencies:
  - python=3.11.7
  - pip:
    - ocr-ops
    - pandas
    - pokemontcgsdk
    - bidict
    - ordered_set
    - requests
    - Pillow==9.5.0
    - tqdm
